<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/adminDrawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <!-- Main Content Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="#F5F6FA">

        <!-- ══ CLEAN ADMIN HEADER ══ -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:background="@drawable/bg_admin_hero"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:paddingTop="48dp"
            android:paddingBottom="22dp">

            <!-- Hamburger Button -->
            <ImageButton
                android:id="@+id/btnAdminMenu"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_menu_hamburger"
                android:layout_marginEnd="12dp"
                app:tint="#FFFFFF"
                android:contentDescription="Open Menu"/>

            <!-- Title block -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tvAdminTitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Admin Panel"
                    android:textColor="#FFFFFF"
                    android:textSize="24sp"
                    android:textStyle="bold"
                    android:fontFamily="@font/nunito_extrabold"
                    android:letterSpacing="0.01"/>

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Control Center"
                    android:textColor="#CCFFFFFF"
                    android:textSize="13sp"
                    android:fontFamily="@font/nunito_semibold"
                    android:layout_marginTop="2dp"/>

            </LinearLayout>

            <!-- Thought of Day — text only, pill style -->
            <TextView
                android:id="@+id/btnThoughtOfDay"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Thought"
                android:textColor="#FFFFFF"
                android:textSize="13sp"
                android:textStyle="bold"
                android:fontFamily="@font/nunito_semibold"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:paddingTop="10dp"
                android:paddingBottom="10dp"
                android:background="@drawable/bg_thought_pill"
                android:layout_marginEnd="10dp"/>



        </LinearLayout>

        <!-- Fragment container — fills remaining space -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="#F5F6FA">

            <FrameLayout
                android:id="@+id/adminFragmentContainer"
                android:layout_width="match_parent"
                android:layout_height="match_parent"/>

            <!-- FAB: Upload leads (only on Employees tab) -->
            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/fabAdminUpload"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_alignParentBottom="true"
                android:layout_margin="16dp"
                android:src="@drawable/outline_add_call_24"
                app:backgroundTint="#FF6A00"
                app:tint="#FFFFFF"
                app:elevation="8dp"
                app:fabSize="normal"/>

        </RelativeLayout>

    </LinearLayout>

    <!-- Drawer Navigation Menu -->
    <LinearLayout
        android:layout_width="290dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="12dp"
        android:layout_marginStart="10dp"
        android:background="@drawable/bg_drawer_orange_premium"
        android:elevation="24dp"
        android:orientation="vertical">

        <com.google.android.material.navigation.NavigationView
            android:id="@+id/adminNavView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@android:color/transparent"
            app:headerLayout="@layout/nav_header"
            app:menu="@menu/admin_drawer_menu"
            app:itemTextColor="#333333"
            app:itemIconTint="#FF6A00"
            app:itemRippleColor="#1A000000"
            app:itemBackground="@drawable/nav_item_bg_light"/>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="16dp"
            android:paddingTop="8dp"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:gravity="center_horizontal">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Privacy Policy"
                android:textColor="#FF6A00"
                android:textSize="12sp"
                android:fontFamily="@font/nunito_extrabold"
                android:clickable="true"
                android:focusable="true"
                android:padding="8dp"
                android:background="?attr/selectableItemBackground"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:text="© 2026 Adyapan School. All Rights Reserved."
                android:textColor="#888888"
                android:textSize="10sp"
                android:fontFamily="@font/nunito_semibold"
                android:alpha="0.7"
                android:gravity="center"/>
        </LinearLayout>
    </LinearLayout>

</androidx.drawerlayout.widget.DrawerLayout>
