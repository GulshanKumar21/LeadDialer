<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Full-screen background image -->
    <ImageView
        android:id="@+id/image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:contentDescription="onboarding"/>

    <!-- Bottom gradient overlay for text readability -->
    <View
        android:layout_width="match_parent"
        android:layout_height="320dp"
        android:layout_gravity="bottom"
        android:background="@drawable/bg_onboard_gradient_overlay"/>

    <!-- Text content pinned to bottom -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:paddingStart="32dp"
        android:paddingEnd="32dp"
        android:paddingBottom="180dp">

        <!-- Accent bar -->
        <View
            android:id="@+id/accentBar"
            android:layout_width="48dp"
            android:layout_height="5dp"
            android:layout_marginBottom="18dp"
            android:background="#FF6A00"/>

        <!-- Title -->
        <TextView
            android:id="@+id/tvOnboardTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Smart Lead Management"
            android:textColor="#FFFFFF"
            android:textSize="30sp"
            android:textStyle="bold"
            android:letterSpacing="0.01"
            android:lineSpacingMultiplier="1.1"
            android:shadowColor="#88000000"
            android:shadowDx="0"
            android:shadowDy="2"
            android:shadowRadius="6"
            android:layout_marginBottom="14dp"/>

        <!-- Description -->
        <TextView
            android:id="@+id/tvOnboardDesc"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Description text here"
            android:textColor="#E0FFFFFF"
            android:textSize="15sp"
            android:lineSpacingMultiplier="1.55"
            android:shadowColor="#88000000"
            android:shadowDx="0"
            android:shadowDy="1"
            android:shadowRadius="4"/>

    </LinearLayout>

</FrameLayout>